name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build (${{ matrix.label }})
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows-latest
            args: '--target x86_64-pc-windows-msvc --bundles nsis'
            rust_target: x86_64-pc-windows-msvc
            label: Windows-x64
          - platform: ubuntu-22.04
            args: '--bundles appimage deb'
            rust_target: ''
            label: Linux-x64

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Linux system dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libappindicator3-dev \
            librsvg2-dev \
            libasound2-dev \
            libxdo-dev \
            patchelf

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: pnpm

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust_target || '' }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          configPath: src-tauri/tauri.release.conf.json
          args: ${{ matrix.args }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.label }}
          path: |
            src-tauri/target/**/release/bundle/**/*.exe
            src-tauri/target/**/release/bundle/**/*.AppImage
            src-tauri/target/**/release/bundle/**/*.deb
            src-tauri/target/**/release/bundle/**/*.sig
            src-tauri/target/**/release/bundle/**/*.json
          if-no-files-found: error

  publish:
    name: Publish release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Flatten release files
        run: |
          mkdir -p release-files
          find artifacts -type f \( \
            -name '*.exe' -o \
            -name '*.AppImage' -o \
            -name '*.deb' -o \
            -name '*.sig' \
          \) -exec cp {} release-files/ \;
          ls -la release-files/

      - name: Create fixed-name copies for website downloads
        run: |
          # Windows
          EXE=$(ls release-files/*.exe 2>/dev/null | head -1)
          [ -n "$EXE" ] && cp "$EXE" release-files/VoxPen.Desktop_x64-setup.exe
          # Linux AppImage
          APPIMAGE=$(ls release-files/*.AppImage 2>/dev/null | head -1)
          [ -n "$APPIMAGE" ] && cp "$APPIMAGE" release-files/VoxPen.Desktop_amd64.AppImage
          echo "Fixed-name copies:"
          ls -la release-files/VoxPen.Desktop_* 2>/dev/null || echo "(none created yet)"

      - name: Generate latest.json for Tauri updater
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          TAG="${GITHUB_REF_NAME}"
          BASE_URL="https://github.com/soanseng/voxpen-desktop/releases/download/${TAG}"

          # Find signature files
          WINDOWS_SIG=""
          LINUX_SIG=""

          for f in release-files/*.exe.sig; do
            [ -f "$f" ] && WINDOWS_SIG=$(cat "$f")
          done
          for f in release-files/*.AppImage.sig; do
            [ -f "$f" ] && LINUX_SIG=$(cat "$f")
          done

          # Find artifact filenames
          WINDOWS_FILE=$(ls release-files/*.exe 2>/dev/null | head -1 | xargs basename 2>/dev/null || echo "")
          LINUX_FILE=$(ls release-files/*.AppImage 2>/dev/null | head -1 | xargs basename 2>/dev/null || echo "")

          # Build latest.json
          cat > release-files/latest.json << ENDJSON
          {
            "version": "${VERSION}",
            "notes": "VoxPen Desktop ${TAG}",
            "pub_date": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "platforms": {
              "windows-x86_64": {
                "signature": "${WINDOWS_SIG}",
                "url": "${BASE_URL}/${WINDOWS_FILE}"
              },
              "linux-x86_64": {
                "signature": "${LINUX_SIG}",
                "url": "${BASE_URL}/${LINUX_FILE}"
              }
            }
          }
          ENDJSON

          cat release-files/latest.json

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: 'VoxPen Desktop ${{ github.ref_name }}'
          body: |
            ## Downloads / 下載

            | Platform / 平台 | File / 檔案 | Notes / 說明 |
            |----------|------|-------|
            | **Windows x64** | `.exe` (NSIS installer) | 不需管理員權限 / No admin required |
            | **Linux x64** | `.AppImage` / `.deb` | AppImage 適用所有發行版 |

            ### ⚠️ Windows Security / Windows 安全提示
            This app is **not code-signed**. Windows SmartScreen may show a warning.
            Click **"More info"** → **"Run anyway"** to proceed.

            ### 🍎 macOS
            macOS builds are not yet available. Contributions welcome — see [README](https://github.com/soanseng/voxpen-desktop#contributing).
          draft: false
          prerelease: false
          files: release-files/*
